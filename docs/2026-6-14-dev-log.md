# 2026-06-14 开发日志

## 准备战斗页面重构

- 移除旧按钮, 改用图片按钮
- 48张卡片(6x8), 百分比定位
- 克隆飞行动画(选中/取消)
- 方向键微调, =/-/1/2 调参数

## 新增按钮

- prepare\_1(开始战斗), prepare\_2(弹出菜单)
- 高度等比缩放,动画后显示

## ESC弹窗统一

- 三页面统一button14弹窗
- 背景交互冻结

## 植物配置

- 24+23共48种植物,属性补全

## 优化

- DPI感知, smoothscale, 大图内存释放
- 战斗页sun\_rect默认值修复
- 选中边框改为蓝色

## 交互细节

- 所有按钮(pygame.mask)像素级悬停变亮(BLEND\_RGB\_ADD)
- 弹窗显示时弹出遮罩(160alpha), 背景按钮悬停和点击全部冻结
- prepare\_2弹窗: prepare\_2\_1.png(背景) + 2\_2.png(回首页) + 2\_3.png(关闭)
- ESC弹窗: button14系列, 14\_2(关闭) + 14\_3(退出游戏)
- 按钮按高度百分比缩放(s = sh / ih), 宽自动等比, 不居中
- 三个页面(首页/准备战斗/战斗)均支持ESC弹出退窗

- 所有图片/按钮/卡片/精灵均以窗口高度为基准等比缩放(h*pct)
- 坐标打印也统一使用高度百分比，不再使用宽度
